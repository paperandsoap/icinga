all_hosts += [
<% @nodes.each do |client_node| -%>
    '<%= client_node['hostname'] %>|<%= client_node['lsb']['codename'] %>|site:<%= node['hostname'] %>|<%= client_node.chef_environment %>|<%= client_node['roles'].join("|") %>',
<% end -%>
]